name: publish_stt_recorder_web

on:
  push:
    tags:
      - "stt_recorder_web-v*"

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    uses: ./.github/workflows/publish_pub_dev.yaml
    with:
      package: stt_recorder_web
