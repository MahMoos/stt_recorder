name: publish_stt_recorder_ios

on:
  push:
    tags:
      - "stt_recorder_ios-v*"

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    uses: ./.github/workflows/publish_pub_dev.yaml
    with:
      package: stt_recorder_ios
