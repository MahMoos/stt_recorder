## 0.1.0+1

- Initial platform interface release.
- Added capture and transcript stream contract for federated implementations.
- Added `VoiceCaptureArtifact` as the `stopCapture()` result contract.
- Made artifact `path` optional so implementations can support in-memory
  outputs such as web.
