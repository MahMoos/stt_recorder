## 0.1.0+1

- Initial iOS implementation release.
- Added AVAudioEngine capture and partial transcript events.
- Changed `stopCapture()` to return artifact bytes and metadata instead of only
  a file path.
