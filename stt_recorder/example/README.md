# stt_recorder_example

Demonstrates how to:

- start and cancel a capture session
- listen to partial transcript events
- stop capture and inspect the returned `VoiceCaptureArtifact`

The example can be used to smoke-test Android, iOS, macOS, and web behavior.
