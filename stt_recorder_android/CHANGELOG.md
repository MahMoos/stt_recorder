## 0.1.0+1

- Initial Android implementation release.
- Added PCM WAV recording and partial transcript events.
- Changed `stopCapture()` to return artifact bytes and metadata instead of only
  a file path.
